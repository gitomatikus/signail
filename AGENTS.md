# Repository Instructions

- This project runs through Docker. Use Docker or Docker Compose for builds,
  tests, and other verification.
- Do not run frontend or backend builds with the host's local `npm`.
